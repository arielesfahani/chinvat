# chinvat
This script turns your domestic VPS (e.g., ArvanCloud/ParsPack) into a "clean" DNS relay. By communicating with your VPS on a custom port (like 2053 or 443), you bypass ISP-level DNS filters. The VPS then forwards your tunnel traffic to a healthy domestic resolver within the data center backbone.
